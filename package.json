{
  "name": "portfolio-3d-francesco-bonvino",
  "version": "1.0.0",
  "description": "3D Portfolio — Francesco Bonvino · Frontend Developer",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.163.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.2.0"
  }
}